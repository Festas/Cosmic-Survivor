{
  "name": "cosmic-survivor",
  "version": "1.0.0",
  "description": "A top-down arena survival game. Survive waves of alien creatures on a hostile planet!",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run copy-assets",
    "copy-assets": "copyfiles -f main.js game.js multiplayer-client.js game-integration.js dist",
    "preview": "vite preview",
    "serve": "vite preview",
    "server": "cd server && node index.js",
    "server:install": "cd server && npm install",
    "dev:all": "npm run server:install && concurrently \"npm run dev\" \"npm run server\"",
    "test": "node --test tests/",
    "validate:content": "node tools/content-validator/validate.mjs"
  },
  "keywords": [
    "game",
    "html5",
    "canvas",
    "survival",
    "pwa"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "copyfiles": "^2.4.1",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "pixi.js": "^8.18.1",
    "ws": "^8.20.0"
  }
}
